import 'package:flutter/material.dart';


class CatogoryScreen extends StatelessWidget {
  const CatogoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Category Screen",style: TextStyle(
          color: Colors.white
      ),),
    );
  }
}
